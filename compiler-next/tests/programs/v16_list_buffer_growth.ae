int main() {
    List<Buffer<int>> values = {};
    push(values, Buffer<int>(1, 1));
    push(values, Buffer<int>(1, 2));
    push(values, Buffer<int>(1, 3));
    push(values, Buffer<int>(1, 4));
    return values[0][0] + values[3][0] - 5;
}
