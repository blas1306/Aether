Matrix<T> pair<T:Storable>(T a,T b){return [a,b];}
int main(){
Matrix<Buffer<int>> a=pair(Buffer<int>(1,10),Buffer<int>(1,20));Matrix<bool> b=pair(true,false);if(b[1,1]){return a[1,2][0]-20;}return 1;
}
