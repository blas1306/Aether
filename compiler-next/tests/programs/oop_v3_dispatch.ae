open class Animal {
    public init() {}
    public open int sound() { return 1; }
}
class Dog : Animal {
    public init() : base() {}
    public override int sound() { return 7; }
}
int main() {
    Animal a = Dog();
    return a.sound();
}
