int consume(Buffer<int> value) { return value[0]; }
int main() {
    List<Buffer<int>> values = {Buffer<int>(1,10),Buffer<int>(1,20)};
    if (consume(swap_remove(values,0)) != 10) { return 1; }
    if (values[0][0] != 20) { return 2; }
    return 0;
}
