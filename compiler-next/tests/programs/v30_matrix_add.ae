Matrix<T> add<T:Storable+Copy+Add>(ref Matrix<T> a,ref Matrix<T> b){return *a+*b;}
int main(){Matrix<double>a=[1,2,3;4,5,6];Matrix<double>b=add(&a,&a);
if(rows(b)!=2){return 1;}if(columns(b)!=3){return 2;}if(b[2,3]!=12){return 3;}if(a[1,2]!=2){return 4;}return 0;}
