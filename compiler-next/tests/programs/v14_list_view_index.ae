int main() {
    List<int> values = {10, 20, 30};
    View<int> readable = view(values);
    ViewMut<int> writable = view_mut(values);
    writable[1] = readable[0] + 10;
    return values[0] + values[1] + values[2] - 60;
}
