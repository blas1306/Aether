import Math.LinearAlgebra

A = [3 2; 1 0; 0 0];

m, n = size(A);
p = rank(A);

u = zeros(m, m);
s = zeros(m, n);
v = zeros(n, n);

Vec, Vap = eig(A' * A);

println("Vec = ", Vec);
println("Vap = ", Vap);