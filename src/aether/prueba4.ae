public int sumar(int a, int b) {
    return a + b;
}

while true {
    int a = input("Ingrese el primer numero: ");
    int b = input("Ingrese el segundo numero: ");

    c = sumar(a, b);

    println("$a$ + $b$ = ", c);
}