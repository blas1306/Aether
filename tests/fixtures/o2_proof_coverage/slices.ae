int main() {
    Array<int> array = {1, 2, 3};
    Array<int> arraySlice = array[0:3];
    List<int> list = {1, 2, 3};
    List<int> listSlice = list[1:3];
    return arraySlice.length + listSlice.length;
}
