int append_three(List<int> xs) {
    xs.push(3);
    return 0;
}

List<int> identity(List<int> xs) {
    return xs;
}

int main() {
    List<int> a = {1, 2};
    List<int> b = identity(a);
    int unused = append_three(b);
    if (a.length == 3) {
        return a[2];
    }
    return 1;
}
