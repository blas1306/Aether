import Math

int GenPrimos(int n) {
    suma1 = 0;

    for i in 1:int(n^1.5) {
        suma2 = 0;

        for j in 1:i {
            if (((j - 1)! + 1) % j) == 0 {
                suma2 = suma2 + 1;
            }
        }

        suma1 = suma1 + floor((n/suma2)^(1/n));
    }

    p = 1 + suma1;
    return p;
}

for i in 1:50 {
    println("p_{$i$} = ", GenPrimos(i));
}