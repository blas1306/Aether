public Exception ArithmeticException(string mensaje) {
    return Exception(mensaje);
}

public double dividir(double a, double b) {
    if (b == 0) {
        throw ArithmeticException("No se puede dividir entre cero");
    }

    return a / b;
}

dividir(10, 0);