int countdown(int start) {
    int current = start;
    int ticks = 0;

    while (current > 0) {
        ticks = ticks + 1;
        current = current - 1;
    }

    return ticks;
}

int main() {
    if (countdown(10000) == 10000) {
        return 0;
    } else {
        return 1;
    }
}
